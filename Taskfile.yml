version: '3'

tasks:
  default:
    cmds:
      - task --list
    desc: 'タスクの一覧を表示'
    silent: true

  new:
    cmds:
      - npx zenn new:article --emoji 🌿 --published false
    desc: '新しい記事を追加'
    silent: true

  list:
    cmds:
      - npx zenn list:articles
    desc: '記事の一覧を表示'
    silent: true

  preview:
    cmds:
      - npx zenn preview
    desc: 'コンテンツのプレビュー'
    silent: true
